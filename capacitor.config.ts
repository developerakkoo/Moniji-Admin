import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Moniji-Admin',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
